<div class="col-md-10 col-md-offset-1">
	<div class="index-wrapper">
		<% @items.each do |item| %> 
			<%= item.title %><br>
			<%= item.description %><br>
		<% end %>
	</div>
</div>

<%= link_to "New", new_item_path %>